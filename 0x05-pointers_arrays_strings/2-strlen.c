#include "main.h"
#include <string.h>

/**
 * _strlen - calculates length of pointed string.
 * @s: input
 * Return: always 0
 */

int _strlen(char *s)
{
	int length = 0;

	while (*s = 0)
	{
		length++;
		s++;
	}

	return (length);
}
